{
  "name": "midnight-vitals",
  "version": "0.1.0",
  "private": true,
  "description": "MidnightVitals — diagnostics for the Midnight ecosystem. Headless probe core (call from ANY project), CLI, and (planned) the React panel extracted from the ZKSplunk fork.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "vitals": "node packages/cli/dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
